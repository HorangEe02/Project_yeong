# OCR 평가 데이터 안내

`real_samples/` (실제 영양제 라벨 촬영 원본, 약 203MB·86장)는 저장소 용량 관리를 위해 git에서 제외되어 있습니다.

- 평가 지표·리포트: [`docs/ocr_baseline_reports/`](../../docs/ocr_baseline_reports/) 에 결과가 정리되어 있습니다.
- 재현이 필요한 경우: 라벨 촬영 이미지를 `real_samples/<카테고리>/` 구조로 배치하면 평가 스크립트가 동일하게 동작합니다.
