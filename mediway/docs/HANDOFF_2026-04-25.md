# MediWay 세션 핸드오프 — 2026-04-25

> 전 세션: [HANDOFF_2026-04-24](./HANDOFF_2026-04-24.md) (이전 프로젝트 루트 문서 참조)
> 브랜치: `mediway/v1.1-t0-1-sync` (origin 푸시 완료, `c936b27`)
> 이전 LIVE: Firebase Hosting `690c50e1e208e60a` · 현 LIVE: `2b52463a288bc8d4`

---

## 1. 한 줄 요약

오늘 세션에서 **T0-1 Phase B (Local Sync) · T1-1/T1-2 (audit/visit_plans 격리) · F5a-c (알림 디스패처 + trigger wiring + preferences UI)** 를 전부 완료. 그 과정에서 발생한 LIVE 호환성 이슈 (legacy write block) 도 `fix(rules)` 로 즉시 완화.

---

## 2. 오늘 commits (33건, 시간 순)

| # | Commit | 범주 | 내용 |
|---|--------|------|------|
| 1  | `1274107` | T0-1 B-1 | E2E HTML 3종 (hospital-isolation / tab-session / wait-queue) |
| 2  | `22b04db` | T0-1 B-1 | `useFcmToken` hook + RTDB persist |
| 3  | `ab85aa7` | T0-1 B-1 | HospitalHomePage 6-tab shell + `/h/:slug/patient/home` |
| 4  | `111d878` | T0-1 B-1 | wait_queue types + `subscribeMyWaitQueue` |
| 5  | `e9a5a38` | T0-1 B-1 | WaitQueueWidget (patient 순번 카드) |
| 6  | `81155c2` | docs | HOSTING_DEPLOY_LOG 신설 |
| 7  | `6be9f3a` | T0-1 B-2 | staff ops (callNextWaiting, markInProgress, markCompleted) |
| 8  | `3093774` | T0-1 B-2 | StaffQueuePage + `/staff/queue` |
| 9  | `edbe495` | rules | wait_queue_by_patient, wait_queue_counters 신설 |
| 10 | `b3bcb3e` | hosting | config restore (SPA rewrites + cache headers) |
| 11 | `33bbd65` | hosting | e2e HTML primary blue 롤백 |
| 12 | `96194ce` | T0-1 B-2 | appointments service + checkInToQueue |
| 13 | `e706d24` | T0-1 B-2 | AppointmentsTab UI (list + form + 접수) |
| 14 | `09cefdd` | hosting | landing → `/h/demo/patient/home` 직접 |
| 15 | `57528f0` | hosting | `Lh='demo'` patch 로그 |
| 16 | `d87adbf` | T0-1 B-2 | chatbot types + sendChatbotMessage |
| 17 | `e6f79a2` | T0-1 B-2 | ChatbotWidget UI |
| 18 | `cc42fef` | chatbot | rate limit 20→25 + UI 뱃지 |
| 19 | `88b3efd` | T0-1 B-2 | preferencesStore (고령자 모드 backbone) |
| 20 | `79ab57e` | T0-1 B-2 | MoreTab 고령자 모드 + body.ui-senior |
| 21 | `153d5e9` | T0-1 B-3 | TODO 주석 + CSS text color 롤백 |
| 22 | `bce279a` | T1-1a | audit_logs → audit_logs_v2 backfill |
| 23 | `a948028` | T1-1b | audit dual-write helper |
| 24 | `ad86f59` | T1-1c | audit cutover (v2 only) |
| 25 | `b47865f` | T1-2b | visit_plans frontend dual-write |
| 26 | `4e64318` | T1-2a | visit_plans backfill + hospitalId 주입 |
| 27 | `63521b4` | T1-2c | visit_plans cutover |
| 28 | `ad02a4c` | rules hotfix | audit_logs legacy read 복구 (admin) |
| 29 | `7ec98f8` | rules hotfix | legacy write 복구 (LIVE 호환) + purge 스크립트 예약 |
| 30 | `d8aada1` | F5a | notification dispatcher infra |
| 31 | `7bf536c` | F5b | trigger wiring (onQueueCall 리팩토링 + onAppointmentCreate + scheduler) |
| 32 | `c936b27` | F5c | MoreTab 알림 설정 카드 (local only) |
| 33 | — | — | 이 문서 (핸드오프) |

---

## 3. LIVE 배포 이력 (오늘)

### Hosting releases (6회)
```
2b52463a288bc8d4  e2e T1-2c 기대값 반영         ← 현 LIVE
770e5eeaddfb1ee9  Lh='demo' 번들 patch
8da78dc2d7001b6c  landing → /h/demo/patient/home
fb6c3ea4c03acbb8  e2e primary blue 롤백
e425dbd4ec15af83  config restore (rewrites + cache)
1f674c8d5ae50dce  landing V2 redirect 초안
─ (이전) ─
690c50e1e208e60a  prod v2 원본
```

### Functions redeploy (총 3회)
1. rate limit 25 (hospitalChatbot)
2. T1-1c cutover + T1-2 관련 (9개 전체)
3. F5b wiring (onAppointmentCreate + scheduler 추가 + onQueueCall refactor)

### Rules deploy (4회)
- T0-1 rules v3 (`wait_queue_by_patient`, `wait_queue_counters`)
- T1-1c cutover (audit_logs write false)
- T1-2c cutover (visit_plans write false)
- 부분 롤백 (audit read admin + legacy write 복구)

### RTDB migrations
- `/audit_logs_v2/{demo|platform}/*` (13 entries backfill + 백업 `audit_logs_backup_1777032201117`)
- `/hospitals/demo/visit_plans/*` (2 entries + hospitalId 주입 + 백업 `visit_plans_backup_1777034578225`)
- `hospitals/demo/profile/themeColor` `#deadbe` → `#004e9f` (2회 복구)

---

## 4. 현재 상태 — 무엇이 LIVE 에 있고 무엇이 로컬에만 있나

### LIVE 에 반영됨
- Rules — audit_logs_v2, wait_queue_by_patient, wait_queue_counters, legacy write 관대처리 재복구
- Functions — 9개 (onQueueCall refactor, onAppointmentCreate 신설, appointmentReminderScheduler 신설, hospitalChatbot rate25, audit dispatcher helper 전파)
- Hosting bundle (prod v2 + 6 surgical patches): landing V2 redirect + config + Lh=demo + e2e blue + e2e T1-2c
- RTDB 데이터: audit_logs_v2, nested visit_plans, themeColor blue

### 로컬에만 있음 (prod parity 재배포 시 일괄 반영 예정)
- `HospitalHomePage` 6-tab shell + 각 탭 placeholder/실구현
- `WaitQueueWidget`, `AppointmentsTab`, `ChatbotWidget`, `MoreTab 고령자 모드`, `StaffQueuePage`
- `useFcmToken` hook
- `preferencesStore`, `notificationPrefsStore`
- `src/services/waitQueue.ts`, `appointments.ts`, `chatbot.ts`, `preferences.ts`, `notificationPrefs.ts`
- `src/services/auditLog.ts` — audit_logs_v2 경로 사용 (LIVE prod 번들은 legacy /audit_logs 읽음)

**따라서 LIVE 번들은 여전히 일부 legacy 경로를 read/write.** 이 이유로 legacy rules 을 부분 복구한 상태.

---

## 5. 예약 작업 (실행 전제 조건 명시됨)

### legacy `/audit_logs` + `/visit_plans` 완전 purge
- 스크립트: `mediway/scripts/purge-legacy-paths.py --dry-run | --apply --confirm`
- 런북: `docs/T1_MIGRATION_LOG.md` § "📅 예약 작업"
- 4가지 전제:
  1. LIVE hosting bundle 이 prod parity 에 도달 + 재배포 완료
  2. 새 프런트가 v2 경로만 사용
  3. Legacy traffic 24h 0건 관측
  4. 백업 `/audit_logs_backup_*`, `/visit_plans_backup_*` 존재

### F5d — 카카오 알림톡 실연결
- 외부 계정 · 발신자 프로필 · 템플릿 심사 (7~14일)
- adapter 파일: `mediway/functions/src/notifications/adapters/alimtalk.ts` (현 stub)

### F5e — SMS / email
- NHN Toast / Aligo / CoolSMS vs SendGrid / SES — provider 선정 필요

### VisitPlan 흰 화면 재현 디버그 (B-3 item 9)
- `src/pages/account/VisitPlanPage.tsx:1` TODO 주석 참고
- prod 번들 모듈 diff 분석 필요

### Hospital slug nested routing (B-3 item 10)
- `src/App.tsx:235` TODO 주석 참고
- HospitalShell 이식 — flat `/patient`, `/staff`, `/admin/*` → `/h/:slug/*` nested

---

## 6. 다음 세션 우선순위 제안

### 🟢 최우선
1. **Phase B-3 item 10 (Hospital slug nested routing)** — 이게 풀려야 prod parity LIVE 재배포가 가능해 legacy purge 해금, F5c UI 도 LIVE 반영
2. **F5d 카카오 알림톡 외부 계정 등록** — 병렬 진행 가능, 심사 7-14일 리드타임

### 🟡 중요
3. F5 dispatcher 모니터링 — `/notification_logs/{uid}/{pushId}` 조회 admin UI 추가
4. AdminDashboardPage 재시도 로직 — Custom Claim 미주입 시 `-` 표시 버그 근본 수정 (세션 중 인지)
5. 대시보드 숫자 "-" 현상 조사 결과 반영 (로그아웃/재로그인 필요 안내를 UI 에 명시)

### 🔵 선택
6. **P3 F8/F9 결제** — 스케줄러 기반 PG 연동 + 대리결제
7. **P3 F1 wait queue UI 통합** — prod 와 local staff queue 차이 reconcile
8. **F5e SMS/email adapter**

---

## 7. 규모 (오늘 누적)

- **33 git commits** on `mediway/v1.1-t0-1-sync`
- **6 hosting releases** + **3 functions deploys** + **4 rules deploys**
- **3 RTDB data migrations**
- **Frontend**: 129 vitest tests pass (49 → 129, +80 신규)
- **Functions**: 90 vitest tests pass (76 → 90, +14 신규)
- **Total test count**: 219
- **tsc 0 errors** across both workspaces
- **보안 감사** (세션 초반 독립 pass): 0 Critical / 0 High

---

## 8. 현재 알려진 이슈

| 이슈 | 심각도 | 비고 |
|------|--------|------|
| admin 대시보드 count "-" | 낮음 | Custom Claim 주입 타이밍 이슈. 로그아웃/재로그인 필요. 근본 수정은 local source AdminDashboardPage 재시도 로직 추가 필요 |
| LIVE 번들 ↔ local source feature 격차 | 중 | prod parity 미달. Phase B-3 item 10 완료 후 hosting 재배포 필요 |
| `themeColor` 재오염 가능 | 낮음 | e2e-hospital-isolation.html 시나리오 #7 이 platformAdmin 로 실행되면 `#deadbe` 로 쓰임. 복구 절차 T1_MIGRATION_LOG.md 에 기록 |
| `/user_fcm_tokens` 0 entries | 관찰 | 실사용 환자 토큰 등록 안 됨. prod 번들의 useFcmToken 동등 로직이 LIVE 에서 실행되는 경로 확인 필요 |
| triageSymptoms 함수 삭제됨 | 중 | "AI 진료과 추천" 위젯이 실제로 동작 안 할 가능성. `LOCAL_SYNC_GAPS.md` § 1.1 기록 |

---

## 9. 중요 경로 reference

### 코드
- Local sync 작업: `src/pages/HospitalHomePage.tsx`, `src/components/patient/tabs/*`
- Wait queue: `src/services/waitQueue.ts`, `src/components/patient/WaitQueueWidget.tsx`, `src/pages/StaffQueuePage.tsx`
- Chatbot: `src/services/chatbot.ts`, `src/components/patient/ChatbotWidget.tsx`, `functions/src/chatbot/`
- Notifications F5: `functions/src/notifications/`, `src/services/notificationPrefs.ts`, `src/stores/notificationPrefsStore.ts`, `src/components/patient/tabs/MoreTab.tsx`
- Preferences (senior mode): `src/stores/preferencesStore.ts`
- Audit v2: `functions/src/util/auditLog.ts`, `src/services/auditLog.ts`
- Appointments: `src/services/appointments.ts`, `functions/src/appointments/`

### 스크립트
- `scripts/migrate-audit-logs-to-v2.py`
- `scripts/migrate-visit-plans-to-nested.py`
- `scripts/purge-legacy-paths.py` (예약)

### 문서
- `docs/T1_MIGRATION_LOG.md` — 마이그레이션 이력 + 예약 작업
- `docs/HOSTING_DEPLOY_LOG.md` — 각 surgical patch 배포 기록
- `docs/LOCAL_SYNC_GAPS.md` — T0-1 분석 + 완료 요약
- `docs/CHATBOT_DESIGN.md`
- `docs/COMMIT_8_RULES.md`, `COMMIT_9_MIGRATION.md`
- `docs/DEPLOY_CHECKLIST.md`

---

## 10. 테스트 계정

- `catlife9029@gmail.com` — platformAdmin (hospitalId=demo, uid=S5gU1edQKeQ1w02th37Q8hQYowI2)
- `admin@demo-hospital.test` / `4DH2K26Zx7LK!5` — demo admin
- `staff-{er,im,gs,ped,os,fm,rad}@demo-hospital.test` — 각 부서 staff (pw 는 세션 로그)
- `p0107044@gmail.com` — 환자

---

## 11. 시작 지시 (다음 세션 프롬프트 예시)

```
MediWay 프로젝트 이어서 진행.
현 브랜치: mediway/v1.1-t0-1-sync (origin 동기화, c936b27)
LIVE hosting: 2b52463a288bc8d4

어제 핸드오프: mediway/docs/HANDOFF_2026-04-25.md

오늘 진행: Phase B-3 item 10 (Hospital slug nested routing) 착수.
HospitalShell 이식 설계부터 상세 계획 작성 후 합의, 단계별 commit.
```

작업 시작 전 이 파일 전체 읽고 맥락 파악 권장.
