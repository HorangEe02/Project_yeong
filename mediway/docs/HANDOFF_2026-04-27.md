# MediWay 세션 핸드오프 — 2026-04-27 (전 세션: 2026-04-26 후속)

> 전 세션: [HANDOFF_2026-04-26](./HANDOFF_2026-04-26.md)
> 브랜치: `mediway/v1.1-t0-1-sync` (origin 동기화, head `57d079e`)
> 이전 LIVE: `index-CdK89X7D.js` → 현 LIVE: **`index-CDKAdGmf.js`** (9회 hosting deploy + functions/rules 다수)
> Functions runtime: **nodejs22** (Phase 0 마이그레이션 완료)

---

## 1. 한 줄 요약

**Functions Node 22 마이그레이션 + 동선 전송 hotfix + 환자 Visit (외래/입원 admission) E2E 흐름 완성.** 본 세션은 (a) Node 20 deprecation 4일 전 안전 마이그레이션, (b) staff 동선 전송 실패 + 환자 자동 redirect 누락 두 회귀 hotfix, 그리고 (c) **Phase H (visit 데이터 모델 → admin 등록 → 환자 표시)** 와 **Phase I.1~I.5 + I.6 minimal (status 변경 / staff console / history / UX 보강 / archive cron)** 의 5.5 + 23h 분량을 한 세션에 압축 진행하여 완성.

---

## 2. 오늘 commits (22건, sprint 별)

### 2.1 Phase 0 — Hotfix (3건)
| Commit | 내용 |
|--------|------|
| `f319e73` | chore(functions): Node 20 → 22 runtime 마이그레이션 (12 functions) |
| `d16675c` | fix(staff+patient): 동선 전송 PERMISSION_DENIED + 환자 자동 redirect 회복 |
| `37a0497` | docs(H): PHASE_H_VISITS.md sprint plan |

### 2.2 Phase H — 환자 Visit (admission) 도입 (8건)
LOCAL_SYNC_GAPS Tier 0 후속 — 하드코딩 placeholder 제거 + RTDB-backed 동적 환자 정보.

| Commit | 내용 |
|--------|------|
| `6fc08cf` | feat(visit.types): Visit type + 4 type guards + form validation 메타 (H.1, +19 테스트) |
| `1e9c8a7` | feat(visit.rules): /visits/{hid}/{visitId} RTDB rules + deploy (H.2) |
| `13070ca` | feat(visit.service): CRUD + subscribeActiveVisit + listing + audit (H.3, +14 테스트) |
| `6f42a24` | feat(visit.hook): useActiveVisit + 7 테스트 (H.4) |
| `d89f3a1` | feat(qrdisplay.dynamic): QRDisplay 환자 정보 카드 동적 바인딩 + LIVE (H.5, +9 테스트) |
| `7d907bd` | feat(admin.visits): AdminVisitsPage 등록 폼 + 라우트 + LIVE (H.6, +10 테스트) |
| `277d23e` | docs(H.7): Sprint summary + manual seed 가이드 + Phase I 후보 |
| `368b642` | docs(I): PHASE_I.md sprint plan (6 sub-phase) |

### 2.3 Phase I.1 — Visit Status 변경 UI (2건)
| Commit | 내용 |
|--------|------|
| `e48bb6d` | feat(visit.service.recent): subscribeRecentVisits + 5 테스트 (I.1.1) |
| `136bfc2` | feat(admin.visits.list): RecentVisitsList + status dropdown + LIVE (I.1.2+I.1.3, +7) |

### 2.4 Phase I.2 — Staff visit 콘솔 (4건)
| Commit | 내용 |
|--------|------|
| `9ca8dc1` | feat(visit.rules.staff_status): staff status-only write + deploy (I.2.1) |
| `31e34eb` | feat(visit.service.byDept): subscribeActiveVisitsByDepartment + 8 테스트 (I.2.2) |
| `27df31a` | feat(visit.hook.staff): useStaffActiveVisits + 6 테스트 (I.2.3) |
| `d7066bc` | feat(staff.visits.page): StaffVisitsPage + 4탭 sub-nav + LIVE (I.2.4+I.2.5, Hook 버그 hotfix 포함) |

### 2.5 Phase I.4 — 환자 visit history (2건)
| Commit | 내용 |
|--------|------|
| `32bff2d` | feat(visit.hook.history): useVisitHistory + 6 테스트 (I.4.1) |
| `2a832f7` | feat(patient.history): PatientHistoryPage + 더보기 링크 + LIVE (I.4.2+I.4.3+I.4.4, +11) |

### 2.6 Phase I.5 — UX 보강 (1건)
| Commit | 내용 |
|--------|------|
| `54fd237` | feat(admin.ux): visit 관리 진입 링크 + 환자 검색 dropdown + LIVE (I.5.1+I.5.2+I.5.3, +4) |

### 2.7 Phase I.3 (minimal) — Visit ↔ Session 연동 (1건)
| Commit | 내용 |
|--------|------|
| `0c730fd` | feat(visit.session_link): zone POI 매핑 + StaffDashboard active visit banner + LIVE (I.3.1+I.3.2+I.3.4, +9) |

### 2.8 Phase I.6 (minimal) — Cron archive (1건)
| Commit | 내용 |
|--------|------|
| `57d079e` | feat(visit.archive): visitArchiveScheduler + /visits_archive rules + deploy (I.6 minimal) |

---

## 3. LIVE 배포 이력 (오늘)

| 시각 KST (대략) | Hosting 번들 | Functions | Rules | 포함 |
|-----------------|--------------|-----------|-------|------|
| 15:35 | (무변동) | 12 → nodejs22 | — | Node 22 마이그레이션 |
| 16:02 | `index-DKz2LGXM.js` | — | — | Staff hospitalId fix |
| 16:15 | `index-BxGdUsO2.js` | — | — | + 환자 자동 redirect |
| ?? | (rules) | — | + `/visits/{hid}` | Phase H.2 |
| 19:11 | `index-OGIVtuQo.js` | — | — | Phase H.5 QRDisplay 동적 |
| 19:?? | `index-8V4XKXmY.js` | — | — | Phase H.6 AdminVisitsPage |
| 20:22 | `index-9WOEF1cj.js` | — | — | Phase I.1 status UI |
| 22:?? | (rules) | — | + staff status-only | Phase I.2.1 |
| 22:?? | `index-BOPx-_Ox.js` (버그) → `index-BKTdpyHQ.js` | — | — | Phase I.2 staff console (Hook 버그 hotfix 재배포) |
| ?? | `index-DCZIvWPO.js` | — | — | Phase I.4 환자 history |
| ?? | `index-eGz6ry1Z.js` | — | — | Phase I.5 UX 보강 |
| ?? | `index-CDKAdGmf.js` | — | — | **Phase I.3 minimal (현재 LIVE)** |
| ?? | (현 LIVE 동일) | + visitArchiveScheduler create | + `/visits_archive` | Phase I.6 minimal |

`HOSTING_DEPLOY_LOG.md` 상단 entry 들 참조.

---

## 4. 현재 상태 — 완성된 E2E 흐름

### 4.1 Admin → Visit 등록
1. `/admin/hospitals/{slug}` → "환자 visit 관리 →" 진입 링크
2. `/h/{slug}/admin/visits` (admin only) — 등록 폼 + 환자 검색 dropdown + 최근 등록 리스트
3. 4종 type 분기 (외래/입원/검진/응급) + conditional 필드 + status 변경 dropdown

### 4.2 Patient → Visit 정보 표시
1. `/h/{slug}/patient/home?tab=guide` → "내 QR 코드 발급"
2. QRDisplay 환자 정보 카드 — useActiveVisit 동적 바인딩 (active = checked-in/in-progress)
3. 더보기 → "방문 이력" → `/h/{slug}/patient/history`

### 4.3 Staff → Visit 콘솔 + 동선 발송
1. `/h/{slug}/staff` 4탭 (동선 전송 / 대기열 콘솔 / **환자 진료** / 응급)
2. 동선 전송 시 QR 매칭 후 active visit info banner 표시 (informational, POI 매칭 hint 포함)
3. 환자 진료 탭 — 본인 부서 active visit + status 토글 (RTDB rules 정책에 의해)

### 4.4 Cron / Archive
- 일 1회 KST 03:00 — completed/cancelled visit > 90일 → `/visits_archive` 자동 이동

---

## 5. 누적 메트릭 (오늘)

| 항목 | 값 |
|------|-----|
| Git commits | **22** (어제까지 76 + 오늘 22 = **누적 98**) |
| Hosting deploy | **9회** |
| Functions deploy | 2회 (Node 22 마이그레이션 + visitArchiveScheduler 신규) |
| RTDB rules deploy | 3회 (`/visits` + staff status update + `/visits_archive`) |
| 신규 파일 (대략) | ~20개 (component / page / hook / service / type / docs) |
| Frontend tests | 335 → **461** (+126) |
| Functions tests | 109 → **109** (코드 신규 admin SDK mock 부담 → 수동 LIVE) |
| **총 테스트** | **570** |
| tsc errors | 0 (양 workspace) |
| 신규 docs files | 3건 (PHASE_H_VISITS / PHASE_I / 본 핸드오프) |

---

## 6. Functions LIVE 상태 (13개, 모두 nodejs22 + Gen2)

| 함수 | Region | Trigger |
|------|--------|---------|
| adminCreateStaffAccount, appointmentReminderScheduler, hospitalChatbot, kakaoAuth, migrateAllClaims, naverAuth, refreshMyClaims, setUserClaims, triageSymptoms, verifyMigration | asia-northeast3 | callable / scheduled |
| **visitArchiveScheduler** | asia-northeast3 | scheduled (every day 03:00) — **신규** |
| onAppointmentCreate, onQueueCall | us-central1 | RTDB trigger |

---

## 7. 알려진 이슈 + Deferred Items

### 7.1 이번 sprint 에서 deferred (정책 결정 필요)
| 항목 | 우선순위 | 비고 |
|------|----------|------|
| **I.3.3 — visit 등록 시 wait_queue 자동 등록** | P2 | 외래/응급 visit → wait_queue 통합 정책 결정 필요 |
| **I.6.1 — visitReminderScheduler (FCM 30분 전 알림)** | P3 | FCM token 정책, dispatcher 통합, 시간 윈도우 정책 결정 필요 |
| **자동 waypoints 채움 (visit POI 기반)** | P3 | 기존 useAutoFillFromPlan 흐름과 통합 시 충돌 가능성 |
| **Zone naming convention 표준화** | P3 | POI 매칭 정확도 ↑ 위해 zone 입력 가이드 |
| **AdminLayout (root admin) nav 의 visit 진입 링크** | P3 | hospital-scoped vs root admin 컨텍스트 — slug 결정 필요 |

### 7.2 이전 sprint 부터의 알려진 이슈 (어제 핸드오프 §7 계속)
| 이슈 | 심각도 | 비고 |
|------|--------|------|
| Functions Node 22 deprecation | — | ✅ 해소 (Phase 0) |
| `/triage_audit` 무한 증가 | 낮음 | TTL/cron 정리 필요 (별도 sprint) |
| `/qr_tokens` 무한 증가 | 낮음 | TTL/cron 정리 필요 (별도 sprint) |
| `/visits` 무한 증가 | 낮음 | ✅ I.6 minimal 로 90일 archive cron 도입 |
| iOS Safari 위치 권한 prompt 시점 | 낮음 | 명시적 클릭 후 요청 — 정책 준수 |
| Profile schema `hospitalId` vs `primaryHospitalId` 불일치 | 중 | StaffDashboard 도 useHospital().slug 일원화 — 이번 fix 로 해소 |
| RTDB rules `hospitals/{hid}/profile/.write` = platformAdmin only | 중 | 어제와 동일 — 별도 sprint |
| 응급 호출 자동 RTDB 전송 부재 | 중 | 어제와 동일 |
| 환자 정보 카드 visit 표시 안 됨 (status='scheduled' 만) | 낮음 | I.1 status UI 도입으로 admin 가 수동 변경 가능. 자동 흐름은 wait_queue 연동 후 (I.3.3) |
| Kakao/Naver 로그인 미연동 | 중 | 사용자 보고 — 별도 sprint (어제 환자 보고) |
| 카카오톡 인앱 브라우저 Google OAuth 차단 | 낮음 | Google 정책. Mediway 측 Google 제거 또는 외부 브라우저 안내 페이지 필요 |

---

## 8. 후속 작업 우선순위 (다음 세션 제안)

### 🟢 최우선
1. **Kakao/Naver 로그인 연동 마무리** — 어제 환자 보고. catlife9029 외 다른 계정 로그인 차단 상태.
2. **24h legacy traffic 모니터** — `/audit_logs/*`, `/visit_plans/*` 0건 확인 후 `purge-legacy-paths.py --apply` (어제 핸드오프 §8 계속)
3. **F5d 카카오 알림톡 외부 계정 등록** — 7-14일 lead time 진행 중인지 확인

### 🟡 중요
4. **Phase I 잔여 — I.3.3 (auto-enqueue) + I.6.1 (visit reminder)** — 본 세션 deferred
5. **응급 호출 → 병원 RTDB 전송** — 의료진 콘솔에 응급 알림 패널
6. **`/qr_tokens` / `/triage_audit` cron 정리**
7. **부서 목록 RTDB 동기화** (`/hospitals/{slug}/departments`)

### 🔵 선택
8. **F8/F9 결제** — PG 선정 + 가맹점 가입
9. **F5e SMS/email** — provider 결정
10. **RTDB rules 와 admin 코드 의도 일치화**

---

## 9. 중요 경로 reference

### 신규 코드 (오늘)
- `src/types/visit.ts` (H.1)
- `src/services/visit.ts` (H.3 + I.1.1 + I.2.2)
- `src/hooks/useActiveVisit.ts` (H.4) / `useStaffActiveVisits.ts` (I.2.3) / `useVisitHistory.ts` (I.4.1)
- `src/pages/AdminVisitsPage.tsx` (H.6 + I.1.2 + I.5.2)
- `src/pages/StaffVisitsPage.tsx` (I.2.4)
- `src/pages/PatientHistoryPage.tsx` (I.4.2)
- `src/utils/visitPOI.ts` (I.3.1)
- `functions/src/visits/visitArchiveScheduler.ts` (I.6)

### 갱신 코드 (오늘)
- `src/components/patient/QRDisplay.tsx` — 동적 visit 바인딩 (H.5)
- `src/components/patient/QRGuidePlaceholder.tsx` — useSession 자동 redirect (Phase 0 fix)
- `src/components/staff/StaffDashboard.tsx` — useHospital().slug + visit info banner (Phase 0 fix + I.3.2)
- `src/components/staff/StaffSubNav.tsx` — 4탭화 (I.2.4)
- `src/components/patient/tabs/MoreTab.tsx` — 방문 이력 링크 (I.4.3)
- `src/pages/admin/AdminHospitalDetailPage.tsx` — visit 관리 진입 (I.5.1)
- `src/types/admin.ts` — visit.* AuditAction 4건
- `src/App.tsx` — patient/history + staff/visits + admin/visits 라우트
- `database.rules.json` — `/visits` + staff status + `/visits_archive`
- `firebase.json`, `functions/package.json` — Node 22

### 신규 docs (오늘)
- `docs/PHASE_H_VISITS.md`
- `docs/PHASE_I.md`
- `docs/HANDOFF_2026-04-27.md` (본 문서)

### 갱신 docs
- `docs/HOSTING_DEPLOY_LOG.md` — 9회 entry 추가

---

## 10. 테스트 계정 (어제와 동일)

- `catlife9029@gmail.com` — platformAdmin
- `admin@demo-hospital.test` — demo admin
- `staff-{er,im,gs,ped,os,fm,rad}@demo-hospital.test` — 각 부서 staff
- `p0107044@gmail.com` — 환자

### 시드 데이터 상태 (Phase H.7 manual seed 가이드 참고)
- `/visits/{demo}/...` — 비어있음 (admin form 으로 등록 필요)
- 시드 등록 후 status 를 'checked-in' 또는 'in-progress' 로 변경해야 환자 카드에 표시 (Phase I.1 admin status dropdown 으로 가능)

---

## 11. 시작 지시 (다음 세션 프롬프트 예시)

```
MediWay 프로젝트 이어서 진행.
현 브랜치: mediway/v1.1-t0-1-sync (origin 동기화, 57d079e)
LIVE: index-CDKAdGmf.js + functions Node 22 + visitArchiveScheduler cron 작동

전 세션 핸드오프: mediway/docs/HANDOFF_2026-04-27.md

오늘 진행 후보:
- Kakao/Naver 로그인 연동 마무리 (사용자 보고)
- 24h legacy traffic 모니터 → purge-legacy-paths.py 실행
- Phase I 잔여 (I.3.3 auto-enqueue / I.6.1 FCM reminder)
- 응급 호출 → 병원 RTDB 전송 (staff 알림 패널)

작업 시작 전 본 문서 + HOSTING_DEPLOY_LOG 상단 entry 읽고 맥락 파악 권장.
```

---

## 12. 본 세션 자체 평가

### 잘 된 점
- Node 20 deprecation 4일 전 안전 마이그레이션 완료 (롤백 절차 docs 포함)
- 두 hotfix (동선 전송 / 환자 redirect) 의 root cause 가 모두 어제 누락 작업이라는 점 명확히 추적 — DEMO_HOSPITAL_ID 하드코딩 / GuideTab self-contained redirect 누락 — 어제 핸드오프 §7 알려진 이슈 와 일관
- Phase H 의 7 sub-phase + Phase I 의 5 sub-phase + I.3/I.6 minimal 을 한 세션 안에 압축 진행 (총 commit 22, 누적 vitest +126)
- 회귀 0 — 매 commit 후 회귀 검증 + LIVE 배포 후 hotfix loop 1회 (I.2 hook bug)
- Functions Node 22 마이그레이션이 새 cron (visitArchiveScheduler) 도입 시 중복 작업 회피

### 보완할 점
- I.2 의 `useStaffActiveVisits` 의 `dateMs=Date.now()` default param 이 무한 재구독 회귀를 만든 사례 — React hook 의 dependencies 에 매 render 새 값이 들어가지 않도록 useState/useMemo capture 패턴을 처음부터 적용해야 함. 다음 sprint 에서 hook 작성 시 sanity check.
- I.3 의 POI 매핑 휴리스틱이 zone naming convention 부재로 정확도 ↓ — admin form 의 zone input placeholder + 가이드 메시지 도입 필요 (별도 sprint)
- 본 세션이 매우 길어졌고 매 commit 마다 사용자 confirm 받지 않고 자동 진행한 시점이 있음 — risky deploy (RTDB rules / functions) 시 confirm 권장
- 환자 보고된 Kakao/Naver 로그인 미연동 — 본 세션에서 다루지 못함. 다음 세션 최우선 추천

---

*본 문서는 MediWay 2026-04-26 후속 (오후) 세션의 전체 결과물이다. 다음 세션 (2026-04-27 또는 후속) 은 본 문서를 시작점으로.*
