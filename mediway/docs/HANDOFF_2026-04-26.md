# MediWay 세션 핸드오프 — 2026-04-26

> 전 세션: [HANDOFF_2026-04-25](./HANDOFF_2026-04-25.md)
> 브랜치: `mediway/v1.1-t0-1-sync` (origin 푸시 완료, head `45963d8`)
> 이전 LIVE: `2b52463a288bc8d4` (surgical patch) → 현 LIVE: `index-CdK89X7D.js` (full deploy)

---

## 1. 한 줄 요약

**LIVE 가 prod parity 도달 + 신규 기능 5건 일괄 이식.** 어제까지 prod-parity 하지 못해 surgical patch 만 배포했던 LIVE 가 오늘 **9회 본 배포** 끝에 local source 와 완전 동기화. 그 위에 **AI Triage / GuideTab 부활 / QR 자가 발급 / 환자 페이지 3 기능 (탭 정렬·외래 필터·응급 호출) / Staff 응급 탭** 신규 기능을 모두 LIVE 에 반영.

---

## 2. 오늘 commits (43건, sprint 별)

### 2.1 B-3.10 — Hospital slug nested routing (6건, 11:15 KST 배포)
| Commit | 내용 |
|--------|------|
| `94077e5` | hospitalProfile service + HospitalProfile 타입 (12 unit) |
| `d0d145d` | HospitalShell + HospitalContext (13 unit, suspended/cross-tenant 가드) |
| `7a7bb53` | nested `/h/:slug/*` + LegacyHospitalRedirect (8 unit) |
| `2eb602d` | Header + HospitalHomePage 가 nested 인식 |
| `5e5f6cc` | App-level routing 통합 smoke (10 케이스) |
| `33daf30` | docs(B-3.10) |

### 2.2 Hosting 본 배포 #1 (1건)
| Commit | 내용 |
|--------|------|
| `1e7c110` | docs(hosting): B-3.10 + F1 결합 본 배포 — `index-D_OWDnbO.js` |

### 2.3 F1 — Wait queue UI 통합 (7건, prod-parity)
| Commit | 내용 |
|--------|------|
| `3174225` | useHospitalFeatures + FEATURE_DEFAULTS (13 unit) |
| `fc4650c` | HospitalHomePage tab visibility from features (10 unit) |
| `b230db5` | WaitQueueWidget hospital-aware + features-gated + empty CTA (15 unit) |
| `bcd9f15` | AppointmentsTab → useHospital().slug |
| `ad4c5d6` | StaffSubNav + Staff/StaffQueue 통합 헤더 + slug 일원화 (6 unit) |
| `b36936b` | wait queue 시나리오 A-D 통합 smoke (6 케이스) |
| `35b64c8` | docs(F1) |

### 2.4 E — AI Triage 재작성 (7건, 11:49 KST 배포)
LOCAL_SYNC_GAPS Tier 0 #3 해소 — 삭제됐던 `triageSymptoms` 함수 source-level 복구.

| Commit | 내용 |
|--------|------|
| `b2ebbb0` | triage types + Zod schema + ALLOWED_DEPARTMENTS 15개 |
| `15e1dd4` | triageSymptoms cloud function (Gemini + epoch-hour rate limit + audit, 19 unit) |
| `d78ffe6` | src/services/triage.ts wrapper + error normalize (15 unit) |
| `1773eb8` | TriageWidget UI (18 unit) |
| `9c4c57a` | HomeTab 통합 + features.aiTriage 가드 |
| `3fc095e` | scenario E 통합 smoke (8 케이스) |
| `8724cbe` | docs(E) + LOCAL_SYNC_GAPS Tier 0 #3 → ✅ |

### 2.5 Hosting 본 배포 #2 — E AI Triage (1건)
| Commit | 내용 |
|--------|------|
| `d6c6ff4` | docs(hosting): triageSymptoms function + Hosting 동시 배포 — `index-paEulSKd.js` |

### 2.6 GuideTab + ChatbotWidget 복구 (4건, 14:13 KST 배포)
LIVE 시각 검증 시 발견한 두 미완 항목 일괄 처리.

| Commit | 내용 |
|--------|------|
| `04e51fb` | GuideTab 14줄 placeholder → 모드 탭 (지도 보기 / QR 안내) + QRGuidePlaceholder |
| `e21b9c2` | GuideTab + QRGuidePlaceholder 단위 테스트 (16 케이스) |
| `48ec43b` | ChatbotWidget hospitalId 소스 일원화 (profile.hospitalId → useHospital().slug) |
| `f8d55bd` | docs(guide+chatbot) |

### 2.7 Hosting 본 배포 #3 (1건)
| Commit | 내용 |
|--------|------|
| `bc2046e` | docs(hosting): GuideTab 부활 + ChatbotWidget fix — `index-IfhnchyD.js` |

### 2.8 QR 자가 발급 + Rate Limit (4건, 14:29 KST 배포)
사용자가 home 탭에서 의료진 데스크 거치지 않고 자가 QR 발급 가능.

| Commit | 내용 |
|--------|------|
| `4ef9147` | RTDB rules `/qr_token_usage` + qrToken service (transaction-based 시간당 30회 cap, 9 unit) |
| `0711b72` | QRGuidePlaceholder 자가 발급 모드 + QRDisplay 통합 |
| `b90ccf1` | 자가 발급 단위 테스트 (15 케이스) |
| `4efab92` | docs(qr) |

### 2.9 Hosting 본 배포 #4 + RTDB rules (1건)
| Commit | 내용 |
|--------|------|
| `732f03f` | docs(hosting): QR 자가 발급 — `index-DwCp7tZA.js` + database rules |

### 2.10 환자 페이지 3 기능 (7건, 15:05 KST 배포)
사용자 요청: ① 탭 가운데 / ② 외래 일·월·년 필터 / ③ 응급 호출.

| Commit | 내용 |
|--------|------|
| `1da6ccf` | HospitalHomePage 6-tab nav `justify-center` |
| `4d28991` | AppointmentsTab 일/월/년 그룹화 + 드롭다운 (5건 미만 자동 숨김) |
| `810cf18` | 그룹화 helper 단위 테스트 (18 케이스 — KST 자정 경계) |
| `49b70cb` | EmergencyCallCard (119 + 위치 + 확인 dialog) |
| `2da2aeb` | GuideTab 「응급 호출」 3rd 모드 + features.emergencyCall 가드 |
| `2a7bc2c` | EmergencyCallCard 단위 테스트 (15 케이스) + tel: 링크 정리 |
| `0ded945` | docs(patient-features) |

### 2.11 Hosting 본 배포 #5 (1건)
| Commit | 내용 |
|--------|------|
| `282f8a8` | docs(hosting): 환자 페이지 3 기능 — `index-BjFBq90O.js` |

### 2.12 응급 호출 활성화 후속 수정 (3건)
LIVE 시각 검증 중 발견 — 응급 호출 노출 안 됨 → 다층 분석 + hotfix.

| Commit | 내용 |
|--------|------|
| `19cd0db` | (15:09 배포) admin FEATURE_KEYS 에 emergencyCall 추가 — 토글 가능하게 |
| `6aafbb7` | (15:15 배포) FEATURE_DEFAULTS.emergencyCall=true — 모든 사용자 default 노출 |
| `45963d8` | (15:21 배포) StaffSubNav 가운데 정렬 + 응급 탭 추가 (`/h/:slug/staff/emergency`) |

---

## 3. LIVE 배포 이력 (오늘 9회 — surgical → full deploy 정책 전환)

| 시각 KST | Hosting 번들 | Functions | Rules | 포함 |
|---------|-------------|-----------|-------|------|
| 11:15 | `index-D_OWDnbO.js` | — | — | B-3.10 + F1 (prod parity 도달) |
| 11:49 | `index-paEulSKd.js` | + `triageSymptoms` (asia-northeast3 신규 create) | — | + Scenario E |
| 14:13 | `index-IfhnchyD.js` | — | — | + GuideTab 부활 + ChatbotWidget fix |
| 14:29 | `index-DwCp7tZA.js` | — | + `/qr_token_usage` | + QR 자가 발급 + Rate Limit |
| 15:05 | `index-BjFBq90O.js` | — | — | + 환자 페이지 3 기능 |
| 15:09 | `index-BIXOAMN-.js` | — | — | + admin emergencyCall 토글 hotfix |
| 15:15 | `index-B7my1M0_.js` | — | — | + emergencyCall default ON |
| **15:21** | **`index-CdK89X7D.js`** | — | — | **+ Staff 응급 탭 + 가운데 정렬 (LIVE 현재)** |

`HOSTING_DEPLOY_LOG.md` 상단 8개 entry 참조.

---

## 4. 현재 상태 — LIVE ↔ Local 일치

### LIVE 에 반영됨 (full source parity)
- B-3.10 nested routing (`/h/:slug/{patient,staff}/*`) + LegacyHospitalRedirect
- F1 wait queue UI 통합 + features 매트릭스
- E AI Triage 재작성 (`triageSymptoms` 함수 + TriageWidget)
- GuideTab 모드 탭 (지도 / QR / 응급)
- QR 자가 발급 + 시간당 30회 rate limit (RTDB rules + transaction)
- 환자 6-tab 가운데 정렬, 외래 일/월/년 필터
- EmergencyCallCard (119 + 위치 + 확인 dialog) — 환자/의료진 양쪽
- Staff sub-nav (동선 전송 / 대기열 콘솔 / 응급) 가운데 정렬

### Functions 변경 (오늘)
- 신규: `triageSymptoms(asia-northeast3)` — Successful create operation

### RTDB rules 변경 (오늘)
- 신규 path: `/qr_token_usage/{uid}/{epochHour}` — self-only write + validate cap 30

### 코드 ↔ LIVE 격차 0
모든 git 커밋이 LIVE 에 반영됨. Surgical patch 폴더 (`/tmp/mediway-patch-staging`) 더 이상 필요 없음.

---

## 5. 누적 메트릭 (오늘)

| 항목 | 값 |
|------|-----|
| Git commits | **43** (어제 33 + 오늘 43 = 누적 76) |
| Hosting 본 배포 | **9회** (어제까지 0회 / 오늘 9회 — 정책 전환) |
| Functions deploy | 1회 (`triageSymptoms` create) |
| RTDB rules deploy | 1회 (`/qr_token_usage` 추가) |
| 신규 파일 (대략) | ~25개 (component / service / test / docs) |
| Frontend tests | 129 → **335 passed** (+206) |
| Functions tests | 90 → **109 passed** (+19) |
| **총 테스트** | **444** |
| tsc errors | 0 (양 workspace) |
| 신규 docs files | 7건 (B-3.10 / F1 / E / GuideTab / QR / Patient features / 본 핸드오프) |

---

## 6. 해결된 LOCAL_SYNC_GAPS (Tier 0 — 모두 ✅)

| # | 작업 | 상태 |
|---|------|------|
| 1 | Rules tightening (cross-tenant 차단) | ✅ T1-1 / T1-2 (어제) |
| 2 | `onQueueCall` 함수 재작성 | ✅ F5b dispatcher 통합 (어제) |
| 3 | `triageSymptoms` 함수 재작성 | ✅ E sprint (오늘) |

Tier 1 (P3) 도 대부분 ✅:
- Hospital slug routing 전체 개편 → ✅ B-3.10
- WaitQueueWidget + StaffQueuePage prod-parity → ✅ F1
- AI Triage Widget → ✅ E

---

## 7. 알려진 이슈

| 이슈 | 심각도 | 비고 |
|------|--------|------|
| Functions Node.js 20 deprecation 경고 | 낮음 | 2026-04-30 deprecation, 2026-10-30 decommission. 별도 sprint 로 Node 22 마이그레이션 |
| `/triage_audit` 무한 증가 | 낮음 | TTL/cron 정리 필요. 별도 sprint |
| `/qr_tokens` 무한 증가 | 낮음 | TTL/cron 정리 필요. 별도 sprint |
| iOS Safari 위치 권한 prompt 시점 | 낮음 | 명시적 클릭 후 요청 — 정책 준수 |
| Profile schema `hospitalId` vs `primaryHospitalId` 불일치 | 중 | F1.1c / E / fix(chatbot) 에서 useHospital().slug 로 일원화 — 현재 ChatbotWidget·WaitQueueWidget·AppointmentsTab·TriageWidget 모두 일관 |
| RTDB rules `hospitals/{hid}/profile/.write` = platformAdmin only | 중 | 코드 측 admin role 도 편집 가능 의도 — 별도 sprint 에서 rules 일치화 (오늘 hotfix 19cd0db 와 별개) |
| 응급 호출 자동 RTDB 전송 부재 | 중 | 의료진 자동 인지 불가. 본 sprint 비범위 — 별도 sprint 가능 |

---

## 8. 후속 작업 우선순위 (다음 세션 제안)

### 🟢 최우선
1. **F5d 카카오 알림톡 외부 계정 등록** — 7-14일 lead time 진행 중인지 확인
2. **24h legacy traffic 모니터** — `/audit_logs/*`, `/visit_plans/*` 0건 확인 후 `purge-legacy-paths.py --apply`
3. **응급 호출 → 병원 RTDB 전송** — 의료진 콘솔에 응급 알림 패널 (별도 sprint)

### 🟡 중요
4. **Functions Node 22 마이그레이션** — 2026-04-30 deprecation 임박
5. **`/qr_tokens` / `/triage_audit` cron 정리** — 무한 증가 방지
6. **부서 목록 RTDB 동기화** (`/hospitals/{slug}/departments`) — 현재 하드코딩

### 🔵 선택
7. **F8/F9 결제** — PG 선정 + 가맹점 가입
8. **F5e SMS/email** — provider 결정
9. **RTDB rules 와 admin 코드 의도 일치화** — admin role 도 hospital profile write 허용
10. **응급실 수용 가능 인원 표시** / 보호자 자동 알림

---

## 9. 중요 경로 reference

### 신규 코드 (오늘)
- `src/components/hospital/{HospitalShell,LegacyRedirect}.tsx` (B-3.10)
- `src/contexts/HospitalContext.tsx` (B-3.10 + features hook in F1)
- `src/services/hospitalProfile.ts` (B-3.10)
- `src/utils/appointmentsGrouping.ts` (외래 필터)
- `src/components/patient/{TriageWidget,QRGuidePlaceholder,EmergencyCallCard}.tsx`
- `src/services/{triage,qrToken}.ts`
- `src/pages/StaffEmergencyPage.tsx`
- `functions/src/triage/*` (types/rateLimit/prompt/triageSymptoms)

### 신규 docs (오늘)
- `docs/B-3_ITEM10_HOSPITAL_SLUG_ROUTING.md`
- `docs/F1_WAIT_QUEUE_UI_INTEGRATION.md`
- `docs/E_AI_TRIAGE_REWRITE.md`
- `docs/GUIDE_TAB_MODE_RESTORE.md`
- `docs/QR_SELF_ISSUE.md`
- `docs/PATIENT_FEATURES_EVAL.md`
- `docs/HANDOFF_2026-04-26.md` (본 문서)

### 갱신 docs
- `docs/HOSTING_DEPLOY_LOG.md` — 9회 배포 entry 추가
- `docs/LOCAL_SYNC_GAPS.md` — Tier 0 #3 + Tier 1 ✅ 표시

---

## 10. 테스트 계정 (어제와 동일)

- `catlife9029@gmail.com` — platformAdmin (display: "박준영", hospitalId=demo)
- `admin@demo-hospital.test` — demo admin (display: "데모 전체 관리자", role=admin, hospitalId=demo)
- `staff-{er,im,gs,ped,os,fm,rad}@demo-hospital.test` — 각 부서 staff
- `p0107044@gmail.com` — 환자

### 응급 호출 정책 (오늘 LIVE)
- demo hospital: `features.emergencyCall=true` (default ON)
- 모든 사용자 (환자 / staff / admin / platformAdmin) 가 응급 호출 사용 가능
- 윤리 안전 장치: 확인 dialog 강제 — 단일 클릭 119 통화 트리거 X

---

## 11. 시작 지시 (다음 세션 프롬프트 예시)

```
MediWay 프로젝트 이어서 진행.
현 브랜치: mediway/v1.1-t0-1-sync (origin 동기화, 45963d8)
LIVE: index-CdK89X7D.js (full source parity 달성)

어제 핸드오프: mediway/docs/HANDOFF_2026-04-26.md

오늘 진행: (예시)
- 24h legacy traffic 모니터 결과 확인 → purge-legacy-paths.py 실행
- 또는 F5d 카카오 알림톡 외부 계정 진행 상황 확인
- 또는 응급 호출 → 병원 RTDB 자동 전송 (staff console 알림 패널)

작업 시작 전 본 문서 + HOSTING_DEPLOY_LOG 전체 읽고 맥락 파악 권장.
```

---

## 12. 본 세션 자체 평가

### 잘 된 점
- prod parity 도달 → 9회 본 배포로 surgical patch 정책 청산
- 각 sprint 가 `(설계 문서 → 합의 → 단계별 commit → 배포 → 시각 검증)` 사이클을 일관되게 유지
- LOCAL_SYNC_GAPS Tier 0 3건 모두 ✅
- 신규 6 sprint 모두 회귀 0 (vitest 444 / tsc 0)

### 보완할 점
- `emergencyCall` 활성화 시 admin FEATURE_KEYS 동기화 빠뜨림 → 시각 검증 후 발견 → hotfix 3건 (19cd0db, 6aafbb7, 45963d8). 향후 features key 추가 시 4 위치 동시 갱신 체크리스트 필요:
  1. `FEATURE_DEFAULTS` (HospitalContext.tsx)
  2. `useFeature` 가드 (consumer)
  3. `FEATURE_KEYS` (AdminHospitalDetailPage.tsx)
  4. (옵션) RTDB rules 검증
- Staff sub-nav 가운데 정렬은 commit 5 (`ad4c5d6`) 부터 좌측 정렬 채택 — 디자인 원칙 사전 명시 부재. 향후 sub-nav 추가 시 정렬 정책 명문화.

---

*본 문서는 MediWay 2026-04-26 단일 세션의 전체 결과물이다. 다음 세션은 본 문서를 시작점으로.*
