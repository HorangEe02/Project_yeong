# AJIN Backend — Cloud Run 슬림 의존성 (Module D + Auth + 일부 Equipment)
# chromadb / langchain / sentence-transformers / torch / ollama 제외 (~3GB 절감).
# Compliance 모듈은 numpy/pandas/scikit-learn/plotly 만 필요 (확인 완료).

# 웹 프레임워크
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
sse-starlette>=2.0.0
python-multipart>=0.0.9    # FastAPI Form/File/UploadFile 의존
email-validator>=2.0.0     # Pydantic EmailStr 의존

# HTTP
requests>=2.28.0
httpx>=0.24.0

# 인증
PyJWT>=2.8.0
bcrypt>=4.0.0
firebase-admin>=6.4.0

# H8 — Cloud Logging → BigQuery audit pipeline.
# /admin/audit/bq-stats 엔드포인트가 사용. ~15MB, slim 빌드에 큰 영향 없음.
google-cloud-bigquery>=3.18.0

# ML/통계 (Module D 분류기·관세 시뮬레이션·SPC)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# 시각화 (Plotly Figure JSON)
plotly>=5.18.0

# 한국어 형태소 (Module D 시나리오 매칭)
kiwipiepy>=0.18.0

# BM25 키워드 검색 (Module A HybridSearcher).
# slim 배포에서 admin/HR 라우트가 features.search 패키지를 거쳐 import 되며 부모
# 패키지(__init__.py) 의 top-level import 가 BM25 를 강제 로드하기 때문에 누락 시
# Module E (HR Stats) 까지 ImportError 로 500 을 낸다.
rank-bm25>=0.2.2

# LLM
# langchain-ollama 은 core/llm_client.py 가 ChatOllama 를 클래스 상속하므로 import-필수.
# 런타임에 OLLAMA_BASE_URL 비워두면 Gemini 로 fallback (llm_router 동작).
langchain-ollama>=0.2.0
langchain-core>=0.3.0
google-genai>=0.5.0

# 문서 내보내기 (Compliance 다운로드 + xlsx + DOCX/PDF/HWPX 템플릿)
python-docx>=1.1.0
fpdf2>=2.8.0
openpyxl>=3.1.0
jinja2>=3.1.0  # DocxExporter / PdfExporter / HwpxExporter 템플릿 렌더 의존
python-hwpx>=2.9.1  # HWPX 생성 (정식 OWPML) — features/draft/hwpx_exporter.py

# 파일 파싱
pypdf>=3.0.0
olefile>=0.46  # v3.6 — Module B 사용자 양식 업로드 시 HWP(5.0) PrvText 추출용

# HTML 파싱 (P2 F11 — BaseCrawler._cached_fetch_html, EU/CarbonESG/EvBattery 라이브 fetch)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# 유틸
python-dotenv>=1.0.0
rich>=13.0.0
psutil>=5.9.0  # 시스템 CPU/메모리 메트릭 (대시보드 우측 패널)
redis>=5.0  # Docker/Cloud Run slim runtime health + PLC gateway status

# Firebase → Supabase/PostgreSQL 운영 전환
SQLAlchemy>=2.0.0
psycopg[binary]>=3.2.0
alembic>=1.13.0
supabase>=2.0.0
