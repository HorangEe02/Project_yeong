# AJIN AI Assistant — 새 섹션 시작 컨텍스트 (v3.10, 2026-05-27 ~05:40 KST)

## 1. 프로젝트 정보
- **경로**: `~/99_me/00_github/04_AJIN/ajin-ai-assistant-react` (nested)
- **부모 경로**: `~/99_me/00_github/04_AJIN`
- **Repo**: `github.com/HorangEe02/ajin-ai-assistant-react` (main 브랜치 기준)
- **사용자**: HorangEe02 / catlife9029@gmail.com
- **본선**: 2026-06-10~11 (KNU SILLI 2026) — **D-15**
- **인프라**: Vercel(frontend) + Cloud Run(backend, asia-northeast3, project `ajin-cb`) + Supabase(ycjuzwltwbeudanjykag)

## 2. 인증 (시연용)
- **URL**: https://ajin-ai-assistant-frontend.vercel.app/login
- **사번**: `admin`
- **비번**: PDF 또는 secret store 에서 직접 확인 (transcript echo 금지)
- 한 번 변경하면 영구 유지 (v3.10 부터 bootstrap reset 비활성 — A+B 패치)

## 3. 절대 규칙 (이 세션 + 이전 세션에서 정의된 전부)
1. **NEVER** push 직접 main — PR 워크플로우 필수 (자동 분류기 차단)
2. **NEVER** "시연 후" 정리 명령을 시연 전 권유 금지 — 명시적으로 "본선(2026-06-11) 종료 후" 표기
3. **NEVER** fallback/workaround 언어 무분별 사용 — 정공법 우선 (architect 상의 권장)
4. **NEVER** Firebase 추가 의존 — Supabase + Vercel 만으로 운영. AUTH_BACKEND env 비활성 유지
5. 비밀번호/secret 은 transcript 에 절대 echo back 금지 — bash inline 도 차단됨
6. Cloud Run env 변경은 자동 새 revision 생성 + 자동 promote 안 됨 → `--to-latest` 명시 필요 (services update-traffic 별도)
7. cloudflared free tunnel 은 free tier idle timeout — 끊기면 `com.ajin.tunnel-watchdog` daemon 자동 회복 + Cloud Run env sync
8. APP_DB_BACKEND=postgres 활성 시 alembic schema + admin user mirror 둘 다 필수
9. Docker compose 의 cwd 가 parent repo 로 빠지는 케이스 — 항상 `cd ~/99_me/00_github/04_AJIN/ajin-ai-assistant-react` 명시
10. Vercel 자동 빌드가 webhook 끊김으로 안 트리거되는 케이스 있음 — `vercel deploy --prebuilt --prod` + `vercel alias set <url> ajin-ai-assistant-frontend.vercel.app` 수동 적용
11. Vercel deploy 시 dashboard `rootDirectory: frontend` 라 repo root 에서 deploy 안 됨 → API `PATCH rootDirectory=null` → deploy → 다시 `rootDirectory=frontend` 복원 패턴
12. Cloud Run alembic job 은 Supabase pgbouncer session pool max 15 에 부딪힘 — 직접 SQL 실행은 classifier 가 차단, **Supabase Dashboard SQL Editor 가 정공법**
13. Production Cloud Run revision 삭제는 사용자 명시 승인 필요 (rollback target 보호)
14. SLOP WARNING 감지 시 정공법으로 전환 — fallback/shim 도입 자제

## 4. 현재 운영 환경 상태 (이번 세션 종료 시점)
| 서비스 | 상태 | 비고 |
|--------|------|------|
| Cloud Run `ajin-backend-00312-yoj` | ✅ 100% traffic | tag=`bootstrap-skip-0532`, smoke PASSED |
| Cloud Run 0% LKG revisions | ✅ 9개 보존 | 00274/00284/00288/00294/00298/00302/00304/00306/00308 + corpus-606 (5/22~5/26) |
| Supabase Postgres `users.photo_url` | ✅ TEXT NOT NULL DEFAULT '' | alembic_version=`20260526_0003` |
| Supabase pgvector chunks | ✅ 974 chunks | 합성 30 + 실제 17 (KOSHA 14 + 법령 1 + MOEL 1 + KATECH 1) |
| Vercel frontend | ✅ `ajin-ai-assistant-frontend-fl3zcwg5r-…vercel.app` | alias 매핑됨 |
| Vercel project `rootDirectory` | ✅ `frontend` | webhook 자동 빌드 정상 |
| Cloud Run env `AUTH_BOOTSTRAP_ADMIN_ENABLED` | ✅ `false` | startup reset 정지 |
| Cloud Run env `AUTH_BOOTSTRAP_ADMIN_PASSWORD` | ✅ 제거됨 | plaintext 노출 해소 |
| Cloud Run env `AUTH_ALLOW_LEGACY_ADMIN` | ⚠️ `true` | 시연용, 시연 후 false |
| Cloud Run env `AUTH_ALLOW_BREAK_GLASS_LOGIN` | ⚠️ `true` | 시연용, 시연 후 false |
| Cloud Run env `AUTH_PRIMARY_PROVIDER` | ⚠️ `local` | 시연용, 시연 후 oidc |
| Docker local (Mac) | ✅ 11 active 컨테이너 | compliance/Supabase local stopped, Celery 4개 유지 |
| Docker disk | ✅ 19.4GB (cache 0B) | 시연 후 `image prune -a` 로 11.21GB 추가 회수 가능 |
| Mac launchd daemons | ✅ `com.ajin.caddy` + `com.ajin.tunnel-watchdog` 둘 다 running | |
| `data/real_documents/` | 137MB (PDF 17개 원본) | pgvector 시드 후 시연 무관, 시연 후 삭제 권장 |
| `.gitignore` | ✅ `data/demo_documents/*.bak.json` 추가됨 | merge 백업 재발 방지 |

## 5. 이번 세션 누적 진행 (v3.5 → v3.10)

### v3.6 — UI 정합성
- `pgvector.py` CAST fix — filter-only 검색 가능 (`backend-00306-wed`)
- ModelSelect 다크모드 색 (var(--hud-text)) + draft.tsx 의 native select 교체
- ModelSelect ↔ 부서 드롭다운 시각 통일 (border-radius 12px, padding 11px 14px, focus var(--hud-primary))
- 이모지 (🅀🅖🅔🅝⭐🇰🇷🌐🖼️🧠) 전체 제거 → family color dot

### v3.7 — 설비 AI 탭 상세
- `EquipmentHoverCard` (호버 미니 카드) + `EquipmentDetailDrawer` (하단 상세 패널) 신규
- `EquipmentLineVisual` — ROT prototype 의 conveyor + 4공정 시뮬레이션 + 7종 설비별 미니 애니메이션
- 7종 설비 KPI 라벨 추가 (프레스 SPM, 용접 Cpk, 로봇 Tact, CNC RPM, 사출 cycle, 레이저 power, Utility uptime)

### v3.8 — 데이터 + 인프라
- KOSHA OSHRI 14건 + 정부 자료 5건 PDF 실제 크롤링 → 17건 success
- `seed_documents.json` 합성 30 보존 + 실제 17 추가 (REAL-KOSHA-*, REAL-LAW-*, REAL-MOEL-*, REAL-KATECH-*)
- pgvector reindex 974 chunks (bge-m3 임베딩 + Supabase upsert)
- `main.tsx` 에 `vite:preloadError` listener — stale chunk 자동 reload
- `vercel.json` rewrites 패턴 개선 (assets 정확히 제외)

### v3.9 — 디지털 사원증
- `DigitalEmployeeBadge.tsx` 신규 — 앞·뒤 3D flip, AJIN SVG 로고 (`/logos/ajin_logo_dark.svg`), QR 클릭 → `/search?tab=people&empId={id}` navigate
- `ProfilePhotoUploadModal.tsx` 신규 — react-image-crop 1:1 + canvas 256×256 JPEG base64
- profile.tsx BASIC 탭 + EmployeeDetailDrawer 에 통합
- search.tsx 에 empId URL param 자동 select (sessionStorage 1회 가드)
- backend `users.photo_url` 컬럼 + `PATCH /api/auth/me/photo` endpoint (revision `00308-vit`, alembic `20260526_0003`)

### v3.10 — 보안 + 안정성
- A: Cloud Run env `AUTH_BOOTSTRAP_ADMIN_ENABLED=false`
- B: `core/auth/database.py` 의 reset 로직에 `must_change_pw=0` skip 가드 + `AUTH_BOOTSTRAP_FORCE_RESET` 명시적 우회 (revision `00312-yoj`)
- `AUTH_BOOTSTRAP_ADMIN_PASSWORD` env plaintext 제거
- Cloud Run 오래된 revision 9개 정리 (00226~00246, 5/20~21)
- Docker compliance 스택 4개 + Supabase local 13개 stop, build cache 3.55GB 회수
- `.bak.json` 백업 파일 정리 + `.gitignore` 등록

## 6. Working Tree 현황 (PR 분리 필요)
```bash
# git status --short 결과 (정리 후)
 M backend/routers/auth.py                       # PATCH /me/photo + photo_url 응답
 M core/auth/database.py                         # photo_url _migrate_columns + bootstrap skip 가드 (B)
 M data/crawled/*.json                           # Celery beat crawler 결과 (10 파일)
 M data/demo_documents/seed_documents.json       # 합성 30 + 실제 17 = 47 docs
 M frontend/package-lock.json                    # react-image-crop 추가
 M frontend/package.json                         # react-image-crop dependency
 M frontend/src/api/me.ts                        # photo_url + updateMyPhoto
 M frontend/src/components/chat/ModelSelect.tsx  # 다크모드 + 시각 통일 + 이모지 제거
 M frontend/src/components/employee/EmployeeDetailDrawer.tsx  # Badge compact
 M frontend/src/routes/chat.tsx                  # ModelSelect 사용 patch
 M frontend/src/routes/draft.tsx                 # native select → ModelSelect
 M frontend/src/routes/profile.tsx               # photo modal trigger
 M frontend/src/routes/search.tsx                # empId URL param + filter-only result
 M frontend/vercel.json                          # rewrites 패턴 개선
 M frontend/src/main.tsx                         # vite:preloadError handler
 M features/search/vector_store/pgvector.py      # CAST(:bind AS TEXT)
 M features/search/searcher.py                   # filter-only path
?? alembic/versions/20260526_0003_add_users_photo_url.py
?? data/crawled/{carbon_esg,domestic_law,eu_regulation,ev_battery,global_trade}/20260526.json
?? data/real_documents/                          # 137MB (.gitignore 등록됨)
?? frontend/public/logos/ajin_logo_{dark,light}.svg
?? frontend/src/components/employee/DigitalEmployeeBadge.tsx
?? frontend/src/components/employee/ProfilePhotoUploadModal.tsx
?? frontend/src/components/equipment/EquipmentDetailDrawer.tsx
?? frontend/src/components/equipment/EquipmentHoverCard.tsx
?? frontend/src/components/equipment/EquipmentLineVisual.tsx
?? scripts/fetch_kosha_real_documents.py
?? scripts/fetch_extra_real_documents.py
?? scripts/merge_real_documents.py
```

**모두 production 에 deploy 됨**. 다만 git 에 commit 안 됨 — PR 분리 작업 남음.

## 7. GitHub 워크플로우 (필수)

### 브랜치 네이밍
- `feat/<scope>-<subject>` (신규 기능)
- `fix/<scope>-<subject>` (버그)
- `chore/<scope>-<subject>` (정리, refactor)

### 커밋 메시지 (Conventional Commits 1.0 + 한글 subject)
```
feat(employee): 디지털 사원증 + 사진 업로드 + QR 검색 navigation 추가

- DigitalEmployeeBadge 앞·뒤 flip + AJIN SVG 로고
- ProfilePhotoUploadModal react-image-crop 1:1
- backend PATCH /api/auth/me/photo + users.photo_url
- alembic 20260526_0003

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

### PR 분리 추천 (작업 단위별)
1. `feat/search-real-corpus` — KOSHA 17건 시드 + pgvector reindex + filter-only fix
2. `feat/ui-model-select-unify` — ModelSelect 다크모드 + draft 교체 + 부서 통일 + 이모지 제거
3. `feat/equipment-detail-visual` — EquipmentHoverCard + Drawer + LineVisual
4. `feat/employee-badge` — DigitalEmployeeBadge + ProfilePhotoUploadModal + QR nav + backend photo_url
5. `fix/auth-bootstrap-skip-on-customized` — must_change_pw=0 skip 가드
6. `chore/vite-preload-error-recover` — main.tsx preloadError + vercel.json rewrites

### 푸시·PR 명령
```bash
git checkout -b feat/employee-badge
git add frontend/src/components/employee/ frontend/src/routes/profile.tsx frontend/public/logos/ backend/routers/auth.py core/auth/database.py alembic/versions/20260526_0003_*.py
git commit -m "..."
git push -u origin feat/employee-badge
gh pr create --title "feat(employee): 디지털 사원증 + 사진 업로드" --body "..."
# 사용자가 GitHub 에서 squash merge
```

### 절대 금지
- `git push --force` 또는 `--force-with-lease` to main
- `git push origin main` 직접
- `--no-verify` (pre-commit hook 우회)

## 8. 시연 직전 권장 task (우선순위)
| # | Task | 예상 시간 |
|---|------|---------|
| 1 | **사용자 admin 로그인 + 비번 변경 (connection pool free 후)** | 5분 |
| 2 | working tree 변경 → 6개 PR 으로 분리 commit + push | 1-2시간 |
| 3 | 시연 검증 — frontend, backend, photo upload, QR nav 전체 E2E | 1시간 |
| 4 | ajin-tunnel healthcheck wget → curl/cloudflared --version 수정 (선택) | 10분 |
| 5 | Vercel ↔ GitHub webhook 복구 (자동 빌드 8h 침묵 이력 있음) | 30분 |
| 6 | 테스트 계정 33명 확보 — 현재 admin 1 + SQLite seed 12 + Postgres mirror 1, 19명 부족 | 1일 |
| 7 | YOLO 비전 검사 (Feature F R-1) — CNC 불량 이미지 검출 | 2-3일 |
| 8 | 시연 시나리오 리허설 (DEMO_SCRIPT.md 따라) | 반나절 |

## 9. 시연 직전 체크리스트 (D-Day, 2026-06-11)
- [ ] `curl https://ajin-cb.web.app/api/health` → `status:ok, llm_connected:true, pgvector_connected:true, pgvector_doc_count >= 974`
- [ ] `curl https://ajin-cb.web.app/api/export/health` → `engines.python_hwpx:true`
- [ ] admin 로그인 → 비번 정상 동작 (must_change_pw=0 유지)
- [ ] `/search` → DOCUMENTS 탭 → "직업병 사례 자동차 노동자" 검색 → REAL-KOSHA-* 1~3위
- [ ] `/search` → DOCUMENTS 탭 → 검색어 비움 + 문서유형 "ECN" 선택 → 351 chunks 중 k 개 표시 (filter-only path)
- [ ] `/draft` → 이메일 초안 생성 → ModelSelect 부서 드롭다운과 시각 일관, 다크모드 글씨 잘 보임
- [ ] `/profile` → BASIC 탭 → 사원증 표시 (AJIN SVG 로고) → 사진 업로드 → 사원증 즉시 반영 → 새로고침 후 유지
- [ ] 사원증 QR 클릭 → `/search?tab=people&empId=admin` → 본인 row 자동 선택
- [ ] `/equipment` → 7개 장비 카드 호버 → mini card 표시 → 클릭 → Drawer + LineVisual 애니메이션
- [ ] `/compliance/reg/iso` → 변경 이력 + 사내 영향 카드 표시
- [ ] Mac 호스트: `pgrep cloudflared && curl <last_tunnel_url>/api/tags -H "X-AJIN-Secret: ..."` → HTTP 200
- [ ] `docker compose -f docker-compose.celery.yml ps` → 4 컨테이너 healthy

## 10. 시연 후 정리 task (2026-06-11 18:00 KST 이후)
```bash
# 1. AUTH 환경변수 정리 (IdP 복원)
gcloud run services update ajin-backend --region=asia-northeast3 --project=ajin-cb \
  --remove-env-vars="AUTH_ALLOW_LEGACY_ADMIN,AUTH_ALLOW_BREAK_GLASS_LOGIN,AUTH_BOOTSTRAP_ADMIN_ENABLED" \
  --update-env-vars="AUTH_PRIMARY_PROVIDER=oidc"

# 2. data/real_documents/ 정리 (PDF 원본 137MB, pgvector 에 이미 시드됨)
rm -rf data/real_documents/

# 3. Docker unused images 정리 (11.21GB 추가 회수)
docker image prune -a -f

# 4. Cloud Run LKG 더 정리 (00274 ~ 00298 가장 오래된 5개)
for rev in ajin-backend-00274-wib ajin-backend-00284-jih ajin-backend-00288-kof \
           ajin-backend-00294-yop ajin-backend-00298-mar; do
  gcloud run revisions delete "$rev" --region=asia-northeast3 --project=ajin-cb --quiet
done

# 5. Supabase local volumes 정리 (local dev data 소실)
docker volume ls --filter label=com.supabase.cli.project=ajin-ai-assistant-react -q | xargs docker volume rm
```

## 11. 미해결 위험 / 모니터링
- **Ollama tunnel 안정성**: free tier cloudflared quick tunnel 수시 끊김 — watchdog 자동회복 의존, 실패 시 사용자가 직접 `cloudflared` 재시작
- **Vercel webhook 침묵**: GitHub push 후 자동 배포 안 되는 케이스 — `vercel deploy --prebuilt --prod` 수동 fallback 명령 준비
- **pgbouncer session pool max 15**: backend deploy 직후 ~5분간 connection peak — 비번 변경 등 실패 가능. Supabase Dashboard 의 Pool Size 30 으로 영구 상향 권장
- **management?cat=system UI 의 Celery Beat**: prod 기준 Cloud Run 컨테이너 안에 pidfile 없음 — unknown 표시 (로컬 dev 환경에서만 OK)

## 12. 자주 사용하는 명령
```bash
# Backend 재배포 (canary slim)
cd ~/99_me/00_github/04_AJIN/ajin-ai-assistant-react
bash scripts/deploy-backend.sh --tag "<name>"

# Frontend 재배포 (prebuilt + alias)
cd frontend
rm -rf .vercel/output dist && vercel build --prod
# rootDirectory 일시 null
python3 -c "<PATCH rootDirectory=null>"
vercel deploy --prebuilt --prod
# rootDirectory 복원
python3 -c "<PATCH rootDirectory=frontend>"
vercel alias set <new-deploy-url> ajin-ai-assistant-frontend.vercel.app

# Cloud Run env 변경
gcloud run services update ajin-backend --region=asia-northeast3 --project=ajin-cb \
  --update-env-vars="KEY=value"
gcloud run services update-traffic ajin-backend --region=asia-northeast3 --project=ajin-cb \
  --to-latest

# Cloud Run rollback
gcloud run services update-traffic ajin-backend --region=asia-northeast3 --project=ajin-cb \
  --to-revisions=ajin-backend-00306-wed=100

# 사용자 admin 비번 변경 (UI 만 지원, gcloud 우회 불가)
# https://ajin-ai-assistant-frontend.vercel.app/login → admin → 변경 → 영구 유지

# Supabase SQL 직접 실행 (alembic 실패 시)
# https://supabase.com/dashboard/project/ycjuzwltwbeudanjykag/sql/new

# Mac local 시연 머신 재가동
supabase start                # local dev DB
docker compose up -d          # local compliance stack
launchctl kickstart -k gui/501/com.ajin.caddy       # 로컬 Caddy
launchctl kickstart -k gui/501/com.ajin.tunnel-watchdog  # cloudflared watchdog
```

## 13. 마지막 main commit
- `2572e32` fix(celery): docker-compose --pidfile + runtime gitignore (push 안 됨, working tree 변경 사항 별도 PR 필요)

---
